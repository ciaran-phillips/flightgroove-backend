const assert = require('assert');

describe('CurrencyFormatter', function () {
    describe('#format()', function () {
        const CurrencyFormatter = require('../../api/helpers/currencyFormatter.js');
        const formatter = new CurrencyFormatter();

    });
});
        